@namespace Exentials.MdcBlazor
@inherits MdcInputTextComponentBase
@if (Outlined)
{
<label @ref="Ref" class="mdc-text-field mdc-text-field--outlined @CssAttributes">
    <span class="mdc-notched-outline">
        <span class="mdc-notched-outline__leading"></span>
        <span class="mdc-notched-outline__notch">
            @if (!NoLabel)
                {
            <span class="mdc-floating-label" id="@LabelId">@Label</span>
            }
        </span>
        <span class="mdc-notched-outline__trailing"></span>
    </span>
    @if (!string.IsNullOrEmpty(Prefix))
    {
    <span class="mdc-text-field__affix mdc-text-field__affix--prefix">@Prefix</span>
    }
    <input type="text" class="mdc-text-field__input" aria-labelledby="@LabelId" disabled="@Disabled" />
    @if (!string.IsNullOrEmpty(Suffix))
    {
    <span class="mdc-text-field__affix mdc-text-field__affix--suffix">@Suffix</span>
    }

</label>
}
else
{
<label @ref="Ref" class="mdc-text-field mdc-text-field--filled @CssAttributes">
    <span class="mdc-text-field__ripple"></span>
    @if (!NoLabel)
        {
    <span class="mdc-floating-label" id="@LabelId">@Label</span>
    }
    @if (!string.IsNullOrEmpty(Prefix))
    {
    <span class="mdc-text-field__affix mdc-text-field__affix--prefix">@Prefix</span>
    }
    <input class="mdc-text-field__input" type="text" aria-labelledby="@( NoLabel? null: Id)" disabled="@Disabled" />
    @if (!string.IsNullOrEmpty(Suffix))
    {
    <span class="mdc-text-field__affix mdc-text-field__affix--suffix">@Suffix</span>
    }
    <span class="mdc-line-ripple"></span>
</label>
}